{
  "name": "@archive/ingest-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/entry.ts --bundle --platform=node --target=node22 --format=esm --outfile=api/ingest.js --banner:js=\"import{createRequire as __cr}from'module';const require=__cr(import.meta.url);\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@archive/db": "workspace:*",
    "@archive/ingest": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
