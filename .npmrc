engine-strict=false
# Workspace packages are consumed as TypeScript source (no build step); see
# packages/*/package.json "exports". Keep them linked, never resolved from the registry.
link-workspace-packages=true
prefer-workspace-packages=true
